apply plugin: 'com.android.library'

version = 1
group = "pitao.sherwin.com.tictactoe"

android {
    compileSdkVersion 21
    buildToolsVersion  "22.0.1"

    defaultConfig {
        minSdkVersion 9
    }

    lintOptions {
        abortOnError false
    }
}

dependencies {
    compile project(':library-material')
}
